# v0.6 キャラクターデザインシート連動版

## 変更内容

- デザインシートから文字の入っていないキャラクター素材を状態別に切り出しました。
- 1枚の固定画像を揺らす方式をやめ、状態に応じてポーズそのものが切り替わります。
- 自分が読書中: 読書ポーズとページをめくるポーズが交互に表示されます。
- 相手が読書中: 応援ポーズになります。
- 元気・大喜び・待機・昼寝で、それぞれ専用ポーズになります。
- 通読ページなどに表示する小型のワンちゃんも同じデザインへ統一しました。
- Firestore参照は `db!` またはnull判定後のローカル変数を使い、以前の `Firestore | null` ビルドエラー対策を維持しています。

## 反映

プロジェクトへ全ファイルを上書き後:

```powershell
git add .
git commit -m "Use character sheet mascot states"
git push
```
